#include <stdlib.h>
#include <ctype.h>
#include <stdbool.h>
#include "parser.h";

//void getToken(FILE* fd, int* type_ptr, char* att_ptr);

/*typedef enum
{
	IDENTIFIER, // 0
	OPERATOR,
	DOPERATOR,
	BRACKET_START,
	BRACKET_END,
	INTEGER,
	DOUBLE,
	STRING
} tType;*/

/*typedef struct
{
	//int id;
	tType type;
	char* value;
} tToken;*/